#include<stdio.h>

void numType(int);

void main() {
    int x;
    printf("Enter Value of x: ");
    scanf("%d", &x);
    numType(x);
}

void numType(int x) {
    //check whether number is prime, armstrong, or perfect number
}

